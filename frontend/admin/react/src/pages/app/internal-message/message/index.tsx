import { Card, Empty } from 'antd';

/**
 * 内部消息列表页面
 */
const InternalMessageList = () => {
  return (
    <Card>
      <Empty description="内部消息列表页面 - 开发中" />
    </Card>
  );
};

export default InternalMessageList;
