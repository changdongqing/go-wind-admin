# 04 · 后端实现设计（v2.0）

> 本章给出三层（Ent Schema / Repo / Service）的代码骨架与落点，风格严格对齐已落地的 `unit_category` / `unit` 模块。

---

## 1. Ent Schema

Schema 骨架见 [02 数据模型设计](./02-数据模型设计.md) 第 2 节，此处仅补充**实现注意事项**。

### 1.1 文件落点

```
backend/app/admin/service/internal/data/ent/schema/
└── category.go     # 新增（单文件承载多 kind）
```

### 1.2 enum 转换器（关键）

`Category.kind` 是 enum 字段，Repo 必须注册 `EnumTypeConverter`（参照 `unit_repo.go` 的 `conversionTypeConverter`）：

```go
kindConverter := mapper.NewEnumTypeConverter[
    thingmodelV1.CategoryKind, category.Kind,
](thingmodelV1.CategoryKind_name, thingmodelV1.CategoryKind_value)
r.mapper.AppendConverters(kindConverter.NewConverterPair())
```

> 未来扩展 kind 枚举值（如追加 DOCUMENT/CERTIFICATE/FORM）时，**proto 与 ent schema 同步追加**，converter 无需改动（基于 name/value 反射）。

### 1.3 自引用 edge 注意

```go
edge.To("children", Category.Type).
    From("parent").
    Field("parent_id").
    Unique().
    Annotations(entsql.Annotation{ OnDelete: entsql.Restrict })
```

- `Field("parent_id")` 显式声明外键列名。
- `OnDelete: Restrict` 由数据库执行——但仍建议 Service 层主动 has-children 校验，给出友好错误码 `CATEGORY_HAS_CHILDREN`。

### 1.4 生成命令

```bash
cd backend && gow ent      # 或 make ent
```

---

## 2. Repository

### 2.1 文件落点

```
backend/app/admin/service/internal/data/
└── category_repo.go   # 新增（镜像 unit_category_repo.go + 子树查询辅助）
```

### 2.2 `category_repo.go` 骨架

```go
package data

import (
    "context"
    "strings"

    "entgo.io/ent/dialect/sql"
    "github.com/go-kratos/kratos/v2/log"
    "github.com/tx7do/kratos-bootstrap/bootstrap"

    paginationV1 "github.com/tx7do/go-crud/api/gen/go/pagination/v1"
    entCrud "github.com/tx7do/go-crud/entgo"
    "github.com/tx7do/go-utils/copierutil"
    "github.com/tx7do/go-utils/mapper"

    "go-wind-admin/app/admin/service/internal/data/ent"
    "go-wind-admin/app/admin/service/internal/data/ent/category"
    "go-wind-admin/app/admin/service/internal/data/ent/predicate"

    thingmodelV1 "go-wind-admin/api/gen/go/thingmodel/service/v1"
)

type CategoryRepo struct {
    entClient *entCrud.EntClient[*ent.Client]
    log       *log.Helper

    mapper        *mapper.CopierMapper[thingmodelV1.Category, ent.Category]
    kindConverter *mapper.EnumTypeConverter[thingmodelV1.CategoryKind, category.Kind]

    repository *entCrud.Repository[
        ent.CategoryQuery, ent.CategorySelect,
        ent.CategoryCreate, ent.CategoryCreateBulk,
        ent.CategoryUpdate, ent.CategoryUpdateOne,
        ent.CategoryDelete,
        predicate.Category,
        thingmodelV1.Category, ent.Category,
    ]
}

func NewCategoryRepo(ctx *bootstrap.Context, entClient *entCrud.EntClient[*ent.Client]) *CategoryRepo {
    repo := &CategoryRepo{
        log:       ctx.NewLoggerHelper("category/repo/admin-service"),
        entClient: entClient,
        mapper:    mapper.NewCopierMapper[thingmodelV1.Category, ent.Category](),
        kindConverter: mapper.NewEnumTypeConverter[
            thingmodelV1.CategoryKind, category.Kind,
        ](thingmodelV1.CategoryKind_name, thingmodelV1.CategoryKind_value),
    }
    repo.init()
    return repo
}

func (r *CategoryRepo) init() {
    r.repository = entCrud.NewRepository[
        ent.CategoryQuery, ent.CategorySelect, ent.CategoryCreate, ent.CategoryCreateBulk,
        ent.CategoryUpdate, ent.CategoryUpdateOne, ent.CategoryDelete,
        predicate.Category, thingmodelV1.Category, ent.Category,
    ](r.mapper)
    r.mapper.AppendConverters(copierutil.NewTimeStringConverterPair())
    r.mapper.AppendConverters(copierutil.NewTimeTimestamppbConverterPair())
    r.mapper.AppendConverters(r.kindConverter.NewConverterPair())
}

// ===== 标准 CRUD：完全镜像 unit_category_repo.go =====
// Count / List / IsExist / Get / Create / Update / Delete / BatchDelete
// SetNillable* 需覆盖：Kind/Code/Level/ParentID/Name/NameEn/Icon/Description/
//   SortOrder/IsEnabled/TenantID
// 注意：Kind/Code 是 immutable，仅在 Create 时 set；Update 中不允许修改 Kind/Code/ParentID/Level（Service 层校验拦截）

// ===== 列表查询：支持 code_prefix 子树过滤 =====

// List 的 ListWithPaging 逻辑（参考 unit_category_repo.go），在 builder 上叠加：
// • 若 query.fieldValues["kind"] 存在 → Where(category.KindEQ(...))
// • 若 query.fieldValues["level"] 存在 → Where(category.LevelEQ(...))
// • 若 query.fieldValues["parent_id"] 存在 → Where(category.ParentIDEQ(...))
// • 若 query.fieldValues["code_prefix"] 存在 → Where(sql 前缀 LIKE)
// • 默认 Order(ent.Asc(category.FieldCode))

func (r *CategoryRepo) buildListPredicates(builder *ent.CategoryQuery, fv map[string]any) {
    if v, ok := fv["kind"].(string); ok && v != "" {
        builder.Where(category.KindEQ(category.Kind(v)))
    }
    if v, ok := fv["level"].(uint32); ok && v > 0 {
        builder.Where(category.LevelEQ(uint8(v)))
    }
    if v, ok := fv["parent_id"].(uint32); ok && v > 0 {
        builder.Where(category.ParentIDEQ(v))
    }
    if v, ok := fv["code_prefix"].(string); ok && v != "" {
        prefix := v
        builder.Where(func(s *sql.Selector) {
            s.Where(sql.Like(s.C(category.FieldCode), prefix+"%"))
        })
    }
    if v, ok := fv["keyword"].(string); ok && v != "" {
        builder.Where(category.Or(
            category.NameContains(v),
            category.CodeContains(v),
        ))
    }
}

// ===== 辅助方法 =====

// GetByCode 按 (kind, code) 取唯一节点（v2 唯一键无需 level）
func (r *CategoryRepo) GetByCode(
    ctx context.Context,
    kind thingmodelV1.CategoryKind,
    code string,
) (*ent.Category, error) {
    return r.entClient.Client().Category.Query().
        Where(
            category.KindEQ(toEntKind(kind)),
            category.CodeEQ(code),
        ).First(ctx)
}

// HasChildren 是否有子节点（删除前校验用）
func (r *CategoryRepo) HasChildren(ctx context.Context, id uint32) (bool, error) {
    n, err := r.entClient.Client().Category.Query().Where(category.ParentIDEQ(id)).Count(ctx)
    return n > 0, err
}

func toEntKind(k thingmodelV1.CategoryKind) category.Kind {
    return category.Kind(thingmodelV1.CategoryKind_name[int32(k)])
}
```

---

## 3. Service

### 3.1 文件落点

```
backend/app/admin/service/internal/service/
└── category_service.go   # 新增（CRUD + code/level 校验）
```

### 3.2 `category_service.go` 骨架

```go
package service

import (
    "context"
    "fmt"
    "regexp"
    "strconv"
    "strings"

    "github.com/go-kratos/kratos/v2/log"
    "github.com/tx7do/go-utils/trans"
    "github.com/tx7do/kratos-bootstrap/bootstrap"
    "google.golang.org/protobuf/types/known/emptypb"
    "google.golang.org/protobuf/types/known/fieldmaskpb"

    "go-wind-admin/app/admin/service/internal/data"
    adminV1 "go-wind-admin/api/gen/go/admin/service/v1"
    thingmodelV1 "go-wind-admin/api/gen/go/thingmodel/service/v1"
    "go-wind-admin/pkg/middleware/auth"
)

var digitsOnly = regexp.MustCompile(`^\d+$`)

type CategoryService struct {
    adminV1.CategoryServiceHTTPServer

    log  *log.Helper
    repo *data.CategoryRepo
}

func NewCategoryService(ctx *bootstrap.Context, repo *data.CategoryRepo) *CategoryService {
    return &CategoryService{
        log:  ctx.NewLoggerHelper("category/service/admin-service"),
        repo: repo,
    }
}

// List / Get —— 透传 repo（略，镜像 unit_category_service）

// Create 创建分类（含 code/level/父子一致性校验）
func (s *CategoryService) Create(ctx context.Context, req *thingmodelV1.CreateCategoryRequest) (*emptypb.Empty, error) {
    if req == nil || req.Data == nil {
        return nil, thingmodelV1.ErrorBadRequest("invalid parameter")
    }
    if err := s.validateCreate(ctx, req.Data); err != nil {
        return nil, err
    }
    operator, err := auth.FromContext(ctx)
    if err != nil {
        return nil, err
    }
    req.Data.CreatedBy = trans.Ptr(operator.UserId)
    return &emptypb.Empty{}, s.repo.Create(ctx, req)
}

// Update 更新分类（不可改 kind/code/parent_id/level）
func (s *CategoryService) Update(ctx context.Context, req *thingmodelV1.UpdateCategoryRequest) (*emptypb.Empty, error) {
    if req == nil || req.Data == nil {
        return nil, thingmodelV1.ErrorBadRequest("invalid parameter")
    }
    if hasMaskedField(req.UpdateMask, "kind", "code", "parent_id", "level") {
        return nil, thingmodelV1.ErrorCategoryImmutableField("kind/code/parent_id/level are immutable")
    }
    operator, err := auth.FromContext(ctx)
    if err != nil {
        return nil, err
    }
    req.Data.Id = trans.Ptr(req.GetId())
    req.Data.UpdatedBy = trans.Ptr(operator.UserId)
    if req.UpdateMask != nil {
        req.UpdateMask.Paths = append(req.UpdateMask.Paths, "updated_by")
    }
    return &emptypb.Empty{}, s.repo.Update(ctx, req)
}

// Delete 批量删除（有子节点或被引用则拒绝）
func (s *CategoryService) Delete(ctx context.Context, req *thingmodelV1.DeleteCategoryRequest) (*emptypb.Empty, error) {
    for _, id := range req.GetIds() {
        hasChildren, err := s.repo.HasChildren(ctx, id)
        if err != nil {
            return nil, err
        }
        if hasChildren {
            return nil, thingmodelV1.ErrorCategoryHasChildren(
                fmt.Sprintf("category id=%d has children", id))
        }
        // reference_count 校验（本期恒 0，预留逻辑）
        // entity, _ := s.repo.GetByID(ctx, id)
        // if entity.GetReferenceCount() > 0 { return ErrorCategoryInUseCannotDelete(...) }
    }
    return &emptypb.Empty{}, s.repo.BatchDelete(ctx, req.GetIds())
}

// ===== 校验 =====

func (s *CategoryService) validateCreate(ctx context.Context, c *thingmodelV1.Category) error {
    // V1: kind 必填且属于允许集合
    if c.GetKind() == thingmodelV1.CategoryKind_CATEGORY_KIND_UNSPECIFIED {
        return thingmodelV1.ErrorBadRequest("kind required")
    }

    // V2: code 必须为纯数字
    code := c.GetCode()
    if !digitsOnly.MatchString(code) {
        return thingmodelV1.ErrorCategoryCodeFormatInvalid("code must be all digits")
    }

    // V3: level 1..4
    lvl := c.GetLevel()
    if lvl < 1 || lvl > 4 {
        return thingmodelV1.ErrorCategoryLevelInvalid("level must be 1..4")
    }

    // V4: code 长度 = level * 2
    if uint32(len(code)) != lvl*2 {
        return thingmodelV1.ErrorCategoryCodeLengthMismatch(
            fmt.Sprintf("code length must be %d for level %d, got %d", lvl*2, lvl, len(code)))
    }

    // V5: name 必填
    if c.GetName() == "" {
        return thingmodelV1.ErrorBadRequest("name required")
    }

    // V6: level=1 不能有 parent；level>1 必须有 parent
    if lvl == 1 {
        if c.GetParentId() != 0 {
            return thingmodelV1.ErrorCategoryParentForbidden("level 1 must not have parent")
        }
    } else {
        if c.GetParentId() == 0 {
            return thingmodelV1.ErrorCategoryParentRequired("level >1 requires parent")
        }
        parent, err := s.repo.GetByID(ctx, c.GetParentId())
        if err != nil || parent == nil {
            return thingmodelV1.ErrorCategoryParentNotFound("parent not found")
        }
        // V7: 父子层级 / kind / 编码前缀 三重校验
        if uint32(parent.Level) != lvl-1 {
            return thingmodelV1.ErrorCategoryLevelParentMismatch(
                fmt.Sprintf("parent level=%d, self level=%d", parent.Level, lvl))
        }
        if string(parent.Kind) != c.GetKind().String() {
            return thingmodelV1.ErrorCategoryKindParentMismatch("parent kind mismatch")
        }
        // V8: code 必须以 parent.code 为严格前缀，且 = parent.code + 2 位数字
        if !strings.HasPrefix(code, parent.Code) || len(code) != len(parent.Code)+2 {
            return thingmodelV1.ErrorCategoryCodePrefixMismatch(
                fmt.Sprintf("code %s must start with parent code %s and length+2", code, parent.Code))
        }
    }
    return nil
}

func hasMaskedField(mask *fieldmaskpb.FieldMask, fields ...string) bool {
    if mask == nil {
        return false
    }
    set := make(map[string]struct{}, len(fields))
    for _, f := range fields {
        set[f] = struct{}{}
    }
    for _, p := range mask.Paths {
        if _, ok := set[p]; ok {
            return true
        }
    }
    return false
}
```

### 3.3 与 v1.0 的代码量对比

| 维度 | v1.0 | **v2.0** |
|------|------|----------|
| Service 校验项数 | 12 | **8**（去掉 path 派生、is_leaf 维护、parent 叶子翻转） |
| 私有辅助函数 | 4（derivePath / updateParentLeafFlag / buildTree / hasImmutable） | **1**（hasMaskedField） |
| RPC 数 | 8（含 ListChildren / GetTree） | **5**（仅 CRUD） |
| 代码总行数估算 | ≈600 | **≈400** |

---

## 4. Wire 依赖注入

### 4.1 注册 ProviderSet

`backend/app/admin/service/internal/data/providers/wire_set.go` 追加：

```go
var ProviderSet = wire.NewSet(
    // ...existing...
    data.NewCategoryRepo,
)
```

`backend/app/admin/service/internal/service/providers/wire_set.go` 追加：

```go
var ProviderSet = wire.NewSet(
    // ...existing...
    service.NewCategoryService,
)
```

### 4.2 HTTP Server 注册

`backend/app/admin/service/internal/server/rest_server.go`：

1. 构造函数参数加入 `categoryService *service.CategoryService`
2. 注册 handler：

```go
adminV1.RegisterCategoryServiceHTTPServer(srv, categoryService)
```

3. 鉴权白名单：CRUD 需登录 + 权限码（`thingmodel:category:create` 等）。

### 4.3 重新生成

```bash
cd backend && gow wire
```

---

## 5. 端到端落地步骤（对照项目流水线）

| 步骤 | 命令 | 产物 |
|------|------|------|
| 1. 写源领域 proto（含错误码追加） | — | `protos/thingmodel/service/v1/category.proto`、`thingmodel_error.proto` |
| 2. 写 BFF proto | — | `protos/admin/service/v1/i_category.proto` |
| 3. 生成 Go API + OpenAPI | `make api && make openapi` | `api/gen/go/thingmodel/.../category*.go`、swagger |
| 4. 写 Ent schema | — | `ent/schema/category.go` |
| 5. 生成 Ent | `gow ent` | `ent/category.go` 等 |
| 6. 写 Repo | — | `data/category_repo.go` |
| 7. 写 Service | — | `service/category_service.go` |
| 8. 注册 Wire + Server | — | `providers/wire_set.go`、`rest_server.go` |
| 9. 生成 Wire | `gow wire` | `wire_gen.go` |
| 10. 编写种子（见 06 章） | — | `data/migrate/seed_thingmodel_category.go` |
| 11. 生成前端 TS | `make ts` | `api/generated/...` |
| 12. 运行验证 | `gow run` | `http://localhost:7788/docs` |

---

## 6. 关键校验汇总（单元测试一对一映射）

| 编号 | 校验 | 错误码 |
|------|------|--------|
| V1 | kind 必填 | `BAD_REQUEST` |
| V2 | code 必须纯数字 | `CATEGORY_CODE_FORMAT_INVALID` |
| V3 | level 必须 1..4 | `CATEGORY_LEVEL_INVALID` |
| V4 | **code 长度 = level × 2** | `CATEGORY_CODE_LENGTH_MISMATCH` |
| V5 | name 必填 | `BAD_REQUEST` |
| V6 | level=1 不可有 parent；level>1 必须有 parent | `CATEGORY_PARENT_FORBIDDEN` / `CATEGORY_PARENT_REQUIRED` |
| V7 | parent.level == self.level - 1 且 parent.kind == self.kind | `CATEGORY_LEVEL_PARENT_MISMATCH` / `CATEGORY_KIND_PARENT_MISMATCH` |
| V8 | code 以 parent.code 为严格前缀 且 len(code) = len(parent.code) + 2 | `CATEGORY_CODE_PREFIX_MISMATCH` |
| V9 | (tenant, kind, code) 唯一 | `CATEGORY_CODE_DUPLICATED`（DB 唯一索引兜底） |
| V10 | Update 不可修改 kind/code/parent_id/level | `CATEGORY_IMMUTABLE_FIELD` |
| V11 | Delete 时 children 不存在 | `CATEGORY_HAS_CHILDREN` |
| V12 | Delete 时 reference_count == 0（本期恒成立） | `CATEGORY_IN_USE_CANNOT_DELETE` |

> 与 v1.0 差异：去掉了"path 一致性"、"is_leaf 翻转"等校验；新增 V4「code 长度 = level × 2」。
